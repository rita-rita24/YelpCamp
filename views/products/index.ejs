<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- <link rel="stylesheet" href="common.css"> -->
  <title>商品一覧</title>
</head>
<body>
  <h1>商品一覧</h1>
  <a href="/products/create">新規作成</a>
  <% for (const p of products) { %>
    <ul>
      <li>
        <p>商品名：<%= p.name %>　</p>
        <a href="/products/<%= p._id %>">詳細</a>
        <a href="/products/edit/<%= p._id %>">編集</a>
        <form action="/products/delete/<%= p._id %>?_method=DELETE" method="POST">
          <button>削除</button>
        </form>
      </li>
    </ul>
  <% } %>
</body>
</html>
